[build]
dockerfilePath = "health_monitoring/Dockerfile"
rootDirectory = ".."

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 300
# startCommand omitted - Railway uses CMD from Dockerfile
