[build]
builder = "dockerfile"
dockerfile = "health_monitoring/Dockerfile"
context = "."

[deploy]
startCommand = "python -m uvicorn health_monitoring.main:app --host 0.0.0.0 --port $PORT"
restartPolicyMaxRetries = 5
restartPolicyMaxRetryDelay = 300
