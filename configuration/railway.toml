[build]
dockerfilePath = "configuration/Dockerfile"
rootDirectory = ".."
watchPaths = ["configuration/", "shared/"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 300

